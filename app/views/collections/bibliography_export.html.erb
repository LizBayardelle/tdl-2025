<% content_for :title, "#{@collection.name} — Annotated Bibliography" %>
<div
  id="collection-bibliography-export-root"
  data-collection-id="<%= @collection.id %>"
  data-collection-name="<%= @collection.name %>"
  data-is-owner="<%= @is_owner %>"
  data-share-permission="<%= @share_permission %>"
></div>

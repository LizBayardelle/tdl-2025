#!/usr/bin/env sh

if gem list --no-installed --exact --silent foreman; then
  echo "Installing foreman..."
  gem install foreman
fi

# Default to port 3000 if not specified
PORT="${PORT:-3000}"

# Parse arguments for -p/--port
while [ $# -gt 0 ]; do
  case "$1" in
    -p|--port)
      PORT="$2"
      shift 2
      ;;
    *)
      break
      ;;
  esac
done

export PORT

exec foreman start -f Procfile.dev --env /dev/null "$@"
